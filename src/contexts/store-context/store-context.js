import React from 'react';
// eslint-disable-next-line no-unused-vars
import DataManager from './data-manager';

/**
 * @type {React.Context<DataManager>}
 */
const StoreContext = React.createContext();


export default StoreContext;
